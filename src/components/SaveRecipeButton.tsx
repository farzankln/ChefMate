// Consolidated: Re-export from ui/SaveButton for backward compatibility
// The ui/SaveButton is the more complete implementation with variants, sizes, and loading states
import { SaveButton } from "@/components/ui/SaveButton";

export { SaveButton };

// Default export for backward compatibility
export default SaveButton;
